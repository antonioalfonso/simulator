import { Pins } from './interface';

/**
 * Pin Mapping
 * @type {Pins}
 *
 * accelerator [analog in]
 * brake       [analog in]
 * swerve      [analog in]
 */
export const pin: Pins = {
  accelerator : 0,
  brake       : 1,
  swerve      : 2
}
